#include <stdio.h>

/**
 * main - compares two variables x and y.
 * Return: Always 0.
 */

int main(void)
{
  int x, y;
  x = y = 0;
  printf("The comparison result is %d\n", x = y);

  return (0);
}
