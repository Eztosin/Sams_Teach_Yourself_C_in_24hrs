#include <stdio.h>

/**
 * main - prints corresponding characters.
 * Return: Always 0.
 */

int main(void)
{
  int x = 72, y = 104;

  printf("The corresponding characters are %c and %c\n", x, y);

  return (0);
}
