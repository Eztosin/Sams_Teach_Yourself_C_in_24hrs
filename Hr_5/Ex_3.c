#include <stdio.h>

/**
 * main - prints intgers in hexadecimal format.
 * Return: 0.
 */

int main(void)
{
  int x, y, z;
  x = 15;
  y = 150;
  z = 1500;

printf("The numbers in hex format are: 0x%04X\n0x%04X\n0x%04X\n", x, y, z);

  return (0);
}
